/**
 * 9. Use trim() to remove any trailing whitespace at the beginning and the end of a string.
 */

let str = "           Torjuman Rimon            ";

console.log(str); // Output:            Torjuman Rimon
console.log(str.trim()); // Output: Torjuman Rimon
