/**
 * 1. Create a variable.js file and declare variables and assign string, boolean, undefined and null data types, Display all the value with there type.
 * */

// declare variables and assign
var str = "Torjuman Rimon";
var bool = true;
var undef = undefined;
var nu = null;

console.log(`${str} type of the variable is --${typeof str}`); // Type of the variable is --string.

console.log(`${str} type of the variable is --${typeof bool}`); // Type of the variable is --boolean.

console.log(`${str} type of the variable is --${typeof undef}`); // Type of the variable is --undefined.

console.log(`${str} type of the variable is --${typeof nu}`); // Type of the variable is --object.
