/**
 * 30. Write a script which generates a random hexadecimal number.
 */

function randomHex() {}

let n = Math.round(Math.random() * 16).toString(16); // we can multiply any number just like 16

console.log(n);
